version: 1
backend:
  phases:
    build:
      commands:
        - cd server
        - npm ci
        - npm run build
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - cd server && npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - server/node_modules/**/* 