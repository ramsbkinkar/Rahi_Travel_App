version: 1
backend:
  phases:
    build:
      commands:
        - cd server
        - npm ci
        - npm run build
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - echo "Current directory:"
        - pwd
        - echo "Running frontend build..."
        - npx vite build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - server/node_modules/**/* 